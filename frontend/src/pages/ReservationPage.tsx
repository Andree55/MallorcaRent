import ReservationForm from '../components/ReservationForm';

const ReservationPage = () => {
    return (
        <div style={{ padding: '2rem' }}>
            <h1>Mallorca Tesla Rental</h1>
            <ReservationForm />
        </div>
    );
};

export default ReservationPage;
